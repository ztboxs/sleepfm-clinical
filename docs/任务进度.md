# SleepFM-Clinical 开发任务进度

> **最后更新**: 2026-03-04

---

## v1.0 基础功能

| 模块 | 状态 | 说明 |
|------|------|------|
| API 核心框架 | ✅ | FastAPI + Uvicorn，端口 6006 |
| 模型管理 | ✅ | 三个模型启动时加载到 GPU |
| GET /api/v1/health | ✅ | 健康检查，含 GPU/RAM 信息 |
| GET /api/v1/task_status | ✅ | 任务状态查询 |
| GET /api/v1/task_result | ✅ | 任务结果获取 |
| GET /api/v1/label_mapping | ✅ | 1065 种疾病标签映射 |
| POST /api/v1/preprocess | ✅ | EDF 预处理 |
| POST /api/v1/embed | ✅ | 嵌入向量生成 |
| POST /api/v1/predict | ✅ | 完整推理（睡眠分期 + 疾病预测） |
| GPU 并发控制 | ✅ | asyncio.Lock 防止 OOM |
| 临时文件管理 | ✅ | 推理完自动清理 |
| Web 前端 | ✅ | 中文 Meta 风格控制台 |
| 结果一致性验证 | ✅ | API vs notebook 100% 一致 |

---

## v2.0 增强功能

| 模块 | 状态 | 说明 |
|------|------|------|
| 年龄真实值输入 | ✅ | 前端输入真实年龄（0~150），后台 `age/100` 归一化 |
| 睡眠阶段筛选 | ✅ | 前端可按 5 个阶段点击筛选查看 |
| 预处理文件导出 | ✅ | `/preprocess` 和 `/predict` 均导出 HDF5 文件 |
| 嵌入文件导出 | ✅ | `/embed` 和 `/predict` 均导出嵌入 HDF5 文件 |
| 推理结果导出 | ✅ | 睡眠分期/疾病预测结果导出为 JSON |
| 文件复用 | ✅ | `preprocessed_file_id` 和 `embedding_file_id` 跳过重复处理 |
| GET /api/v1/download/{file_id} | ✅ | 导出文件下载（24h 自动清理） |
| POST /api/v1/sleep_staging | ✅ | 独立睡眠分期接口 |
| POST /api/v1/disease_prediction | ✅ | 独立疾病预测接口 |
| 任务状态预检查 | ✅ | 前端提交前先检查任务状态，避免上传后才提示 busy |
| 预估剩余时间 | ✅ | `task_status` 返回 `estimated_remaining_seconds` |
| 历史平均耗时 | ✅ | `task_status` 返回 `avg_task_duration_seconds` |
| 文档更新 | ✅ | 接口文档、部署文档、测试指南全部同步更新至 v2.0 |

---

**总结**：v1.0 基础功能 + v2.0 增强功能均已实现。
